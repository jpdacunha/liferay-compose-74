
# -----------------------------------------------------------------------------
# Elasticseaarch settings
# -----------------------------------------------------------------------------
# Elasticsearch HTTP and transport ports exposed on the host
ELASTICSEARCH_HOST_PORT=9200
ELASTICSEARCH_TRANSPORT_HOST_PORT=9300

# -----------------------------------------------------------------------------
# Kibana settings
# -----------------------------------------------------------------------------
# Kibana UI port exposed on the host
KIBANA_HOST_PORT=5601

# -----------------------------------------------------------------------------
# MySQL database settings
# -----------------------------------------------------------------------------
# MySQL port exposed on the host
MYSQL_HOST_PORT=3306
MYSQL_ROOT_PASSWORD=password
MYSQL_USER=liferay
MYSQL_PASSWORD=liferay
MYSQL_DATABASE=lc74lportal

# -----------------------------------------------------------------------------
# Liferay settings
# -----------------------------------------------------------------------------
# Absolute path to the local Liferay license file
LIFERAY_LICENSE_FILE_PATH=/home/dev/activation-key-dxpdevelopment-7.4-inetum.xml
# Liferay web port exposed on the host
LIFERAY_HOST_PORT=8080
# Liferay remote debug port exposed on localhost
LIFERAY_DEBUG_HOST_PORT=11311
